apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: loans-data
  name: loans-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
